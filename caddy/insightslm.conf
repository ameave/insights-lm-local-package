# InsightsLM Caddy Configuration File

# Redirect HTTP to HTTPS
http://{$INSIGHTSLM_HOSTNAME} {
    redir https://{host}{uri} permanent
}

# Insights LM (solo HTTPS)
{$INSIGHTSLM_HOSTNAME} {
    reverse_proxy insightslm:80
}